{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile ABC to MusicXML (Active File)",
            "type": "shell",
            "command": "if [ \"${fileExtname}\" = \".abc\" ]; then ./build.sh \"${file}\"; else echo \"Error: Active file is not an .abc file.\"; exit 1; fi",
            "group": "build",
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "panel": "shared"
            },
            "problemMatcher": []
        },
    ]
}